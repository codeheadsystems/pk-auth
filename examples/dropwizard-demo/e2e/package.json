{
  "name": "@pk-auth/dropwizard-demo-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright E2E tests for the pk-auth Dropwizard demo. Uses Chrome's CDP virtual WebAuthn authenticator.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
